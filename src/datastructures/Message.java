package datastructures;

/**
 * Message interface which allows the encoder, decoder and message handler
 * to take advantage of polymorphism and refer to all message objects by
 * their base type
 */

public interface Message {	

}
